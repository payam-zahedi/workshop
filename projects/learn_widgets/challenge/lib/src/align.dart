import 'package:flutter/material.dart';

class LearnAlign extends StatelessWidget {
  const LearnAlign({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('You should pass this Challenge');
  }
}
